# TODO 앱 PRD 작성 워크시트

아래 힌트만 보고 직접 PRD를 작성해보세요. 완성된 PRD는 Step 4에서
Continue Agent 모드에게 그대로 전달할 예정입니다.

막히면 Continue Chat을 **Chat 모드**로 열고 이렇게 물어봐도 됩니다.
```
아래 힌트를 참고해서 TODO 앱 PRD 초안을 같이 작성해줘.
(이 워크시트 내용을 붙여넣기)
```

---

## 프로젝트 개요
힌트:
- 앱 이름은 자유롭게 정하되, 다음 실습(3-2, Day2)에서도 계속 이어서 쓰니 심플한 이름을 추천합니다.
- 백엔드는 Python 웹 프레임워크 중 하나를 선택하세요. (FastAPI 추천 — 가볍고 자동 문서화가 지원됨)
- 프론트엔드는 별도 프레임워크 없이 HTML/CSS/JS로 작성한다고 명시하세요.
- DB는 별도 설치 없이 파일 하나로 동작하는 가벼운 DB를 선택하세요. (SQLite 추천 — Python 표준 라이브러리 `sqlite3`로 바로 사용 가능)
- 실행 명령어(예: `uvicorn ...`)까지 적어두면 나중에 헷갈리지 않습니다.

작성:
-

## 핵심 기능
힌트:
- TODO 앱이라면 최소한 어떤 동작이 가능해야 할지 4가지를 생각해보세요. (추가 / 조회 / 완료 처리 / 삭제)
- 각 기능이 사용자 입장에서 어떻게 동작하는지(어떤 버튼을 누르면 어떻게 되는지) 한 문장으로 적어보세요.

작성:
1.
2.
3.
4.

## API 명세
힌트:
- 각 기능(추가/조회/완료/삭제)에 어떤 HTTP 메서드가 어울릴지 생각해보세요.
  (조회 → GET, 새로 만들기 → POST, 일부만 수정 → PATCH, 삭제 → DELETE)
- 경로는 다루는 리소스 이름을 그대로 쓰는 것이 일반적입니다.
- **주의**: 이 API 형태는 실습 3-2(고도화)와 Day2 실습에서 그대로 이어서 사용하므로,
  아래 4개 조합(추가/조회/완료 토글/삭제)이 모두 포함되도록 작성하세요.

작성:
- GET
- POST
- PATCH
- DELETE

## 기술 요구사항
힌트:
- DB에 저장할 데이터 항목(컬럼)이 무엇일지 생각해보세요. (제목, 완료 여부, 생성 시각 + 각 항목을 구분할 식별자)
- 프론트엔드가 페이지 새로고침 없이 동작하려면 어떤 방식으로 백엔드와 통신해야 할지 생각해보세요. (브라우저 JS의 fetch API)
- 브라우저가 백엔드 서버로 요청을 보낼 때 필요할 수 있는 설정이 있는지 찾아보세요. (CORS)

작성:
-

## 파일 구조
힌트:
- 백엔드 코드와 DB 처리 코드를 한 파일에 몰아넣을지, 나눌지 생각해보세요.
- 프론트엔드 파일은 어느 폴더에 둘지 정해보세요. (정적 파일을 서비스하는 폴더명)
- 필요한 패키지 목록을 적어둘 파일도 잊지 마세요.

작성:
-
